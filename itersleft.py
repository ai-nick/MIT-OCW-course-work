y = 0
x = 9
itersleft = x # binding itersleft to value of x
while(itersleft>0): # while x is greater than zero
    y = y + x # adding x to itself essentially 
    itersleft = itersleft - 1
    # reduceing itersleft by one until its no longer greater than zero
print y # this is a squaring problem taking x * x essentially
